apiVersion: v1
kind: PersistentVolume
metadata:
  name: hello-web-disk-pv-volume-1
  labels:
    type: MyApp
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"
  persistentVolumeReclaimPolicy: Retain
